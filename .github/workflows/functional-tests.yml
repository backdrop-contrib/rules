name: Simpletest
on: [pull_request]
jobs:
  functional_test:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - name: Run Tests
        uses: backdrop-ops/gha/actions/simpletest@v1
        with:
          module_dependencies: "entity_plus,entity_ui,entity_token"
